import { UserQueryFailedFilter } from './user-query-failed.filter';

describe('UserQueryFailedFilter', () => {
  it('should be defined', () => {
    expect(new UserQueryFailedFilter()).toBeDefined();
  });
});
